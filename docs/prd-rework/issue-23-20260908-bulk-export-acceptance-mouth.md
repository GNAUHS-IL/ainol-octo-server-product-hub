# PRD 返工沉淀：#23 需求单批量导出验收口径

- 问题：批量导出 PRD 初稿将首版导出格式、负责人字段来源、导出数量上限和文件有效期留为待确认，导致 Review 与验收无法判定。
- 修复口径：首版默认 `.xlsx`；负责人优先取需求单当前明确指定的负责人，无明确负责人展示“未分配”；首版单次导出上限 1000 条可见需求单；下载入口有效期为导出完成后 24 小时。
- 检查项：导出 / 报表类 PRD 必须在进入 Review 前明确默认格式、关键字段取值规则、数量上限、文件有效期和超量恢复路径，不得把影响验收的产品口径留作待确认。
